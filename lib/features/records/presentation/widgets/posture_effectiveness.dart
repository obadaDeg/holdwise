import 'package:flutter/material.dart';

class PostureEffectiveness extends StatelessWidget {
  const PostureEffectiveness({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Posture Effectiveness'),
    );
  }
}