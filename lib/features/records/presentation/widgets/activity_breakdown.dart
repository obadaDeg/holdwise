import 'package:flutter/material.dart';

class ActivityBreakdown extends StatelessWidget {
  const ActivityBreakdown({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Activity Breakdown'),
    );
  }
}