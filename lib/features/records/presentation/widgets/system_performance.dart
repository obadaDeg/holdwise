import 'package:flutter/material.dart';

class SystemPerformance extends StatelessWidget {
  const SystemPerformance({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('System Performance'),
    );
  }
}