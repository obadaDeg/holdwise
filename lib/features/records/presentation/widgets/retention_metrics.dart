import 'package:flutter/material.dart';

class RetentionMetrics extends StatelessWidget {
  const RetentionMetrics({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Retention Metrics'),
    );
  }
}