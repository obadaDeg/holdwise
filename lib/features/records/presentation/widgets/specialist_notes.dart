import 'package:flutter/material.dart';

class SpecialistNotes extends StatelessWidget {
  const SpecialistNotes({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Specialist Notes'),
    );
  }
}