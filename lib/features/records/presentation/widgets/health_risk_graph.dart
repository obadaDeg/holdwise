import 'package:flutter/material.dart';

class HealthRiskGraph extends StatelessWidget {
  const HealthRiskGraph({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Health Risk Graph'),
    );
  }
}