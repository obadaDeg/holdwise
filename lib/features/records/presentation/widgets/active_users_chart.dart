import 'package:flutter/material.dart';

class ActiveUsersChart extends StatelessWidget {
  const ActiveUsersChart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Active Users Chart'),
    );
  }
}