import 'package:flutter/material.dart';

class AlertResponseGauge extends StatelessWidget {
  const AlertResponseGauge({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Alert Response Gauge'),
    );
  }
}