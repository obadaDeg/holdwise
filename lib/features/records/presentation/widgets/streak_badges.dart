import 'package:flutter/material.dart';

class StreakBadges extends StatelessWidget {
  const StreakBadges({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Streak Badges'),
    );
  }
}