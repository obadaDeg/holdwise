import 'package:flutter/material.dart';

class PostureHeatmap extends StatelessWidget {
  const PostureHeatmap({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Posture Heatmap'),
    );
  }
}